import React from "react";

const ActivationKey = () => {
  const data = {
    message:
      "Offer 🔥 60% Off. Unlock full features. Get custom colors, new animations, and interactions. Enter license key to continue.",
  };

  return (
    <div>
      <p>{data.message}</p>
    </div>
  );
};

export default ActivationKey;
